{
  "devDependencies": {
    "@azure/static-web-apps-cli": "^0.8.0",
    "vite": "^2.6.7"
  },
  "scripts": {
    "dev": "vite & swa start http://localhost:3000 --api-location api",
    "local:install": "npm install && cd api && npm install && func init --language javascript --worker-runtime node",
    "build": ""
  }
}